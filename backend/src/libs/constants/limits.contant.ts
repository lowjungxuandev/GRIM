export const EXPORT_DEFAULT_LIMIT = 20;
export const EXPORT_MAX_LIMIT = 50;
export const IMPORT_MAX_IMAGE_BYTES = 500 * 1024 * 1024;
/** Max size per prompt file for `PUT /api/v1/prompts` (`extract_text` / `analyzing_text` parts). */
export const PROMPT_FILE_MAX_BYTES = 2 * 1024 * 1024;
