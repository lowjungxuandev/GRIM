name: details
description: "A new Flutter package project."
version: 0.0.1
publish_to: none
environment:
  sdk: ^3.11.5
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# Sync with mobile/pubspec.yaml — latest transitives the Flutter SDK pins older.
dependency_overrides:
  cli_util: ^0.5.0
  _fe_analyzer_shared: ^100.0.0
  analyzer: ^13.0.0
  matcher: ^0.12.20
  meta: ^1.18.2
  native_toolchain_c: ^0.18.0
  test: ^1.31.1
  test_api: ^0.7.12
  test_core: ^0.6.18
  vector_math: ^2.3.0
  xml: ^7.0.1

flutter:
