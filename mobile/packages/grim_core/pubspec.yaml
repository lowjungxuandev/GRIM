name: grim_core
description: Shared core primitives for GRIM mobile packages.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.4

dependencies:
  camera: ^0.11.2+1
  dio: ^5.9.0
  firebase_core: ^4.1.1
  firebase_messaging: ^16.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
